<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cloudpos.demo.cert">

    <!-- android.permission.WIZARPOS_SAFE_MODULE -->
    <uses-permission android:name="android.permission.WIZARPOS_SAFE_MODULE" />
    <!--  android.permission.CLOUDPOS_SAFE_MODULE_READONLY  -->
    <uses-permission android:name="android.permission.CLOUDPOS_SAFE_MODULE_READONLY" />
    <!-- android.permission.WIZARPOS_SAFE_MODULE -->
    <uses-permission android:name="android.permission.CLOUDPOS_SAFE_MODULE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- wake lock -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- reboot -->
    <uses-permission android:name="android.permission.CLOUDPOS_REBOOT" />
    <!-- install silence -->
    <uses-permission android:name="android.permission.CLOUDPOS_INSTALL_SILENCE" />
    <!-- uninstall silence -->
    <uses-permission android:name="android.permission.CLOUDPOS_UNINSTALL_SILENCE" />

    <!-- set time -->
    <uses-permission android:name="android.permission.CLOUDPOS_SETTIME" />

    <!--  <uses-permission android:name="android.permission.WIZARPOS_DISABLE_HOME_KEY"/>
     <uses-permission android:name="android.permission.CLOUDPOS_DISABLE_HOME_KEY"/>  -->
    <!-- android:theme="@android:style/Theme.Translucent.NoTitleBar.Fullscreen"  -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/display_128"
        android:label="@string/app_name">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:label="@string/app_name" />
    </application>

</manifest>
